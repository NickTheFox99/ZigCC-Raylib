{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "request": "launch",
            "name": "Windows Debug",
            "preLaunchTask": "Debug Windows", // This should match the label in tasks.json
            "program": "${workspaceFolder}/bin/win/${fileBasenameNoExtension}.exe",
            "cwd": "${workspaceFolder}"
        },
        {
            "type": "cppdbg",
            "request": "launch",
            "name": "Windows Release",
            "preLaunchTask": "Release Windows", // This should match the label in tasks.json
            "program": "${workspaceFolder}/bin/win/${fileBasenameNoExtension}.exe",
            "cwd": "${workspaceFolder}"
        }
    ]
}
